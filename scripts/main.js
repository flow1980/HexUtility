/*
const navHexWidth     = document.getElementById("nav-hex-tools").style.width;
const navSrecordWidth = document.getElementById("nav-s-record-tools").style.width;
const navOtherWidth   = document.getElementById("nav-other-tools").style.width;

const maxNavWidth = Math.max(navHexWidth, navSrecordWidth, navOtherWidth);
  
document.getElementById("nav-hex-tools").style.width = maxNavWidth;
document.getElementById("nav-s-record-tools").style.width = maxNavWidth;
document.getElementById("nav-other-tools").style.width = maxNavWidth;
*/
  